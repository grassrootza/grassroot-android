<?xml version="1.0" encoding="utf-8"?>
<shape
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- view background color -->
    <solid
        android:color="@color/white" />

    <padding
        android:left="@dimen/padding_card_left"
        android:top="@dimen/padding_small"
        android:right="@dimen/padding_card_left"
        android:bottom="@dimen/padding_small" />

    <corners
        android:radius="@dimen/chat_bar_cnr_radius" />

    <stroke
        android:color="@color/md_grey_400"
        android:width="2dp"/>

</shape>