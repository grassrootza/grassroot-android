<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/gpick_item_root"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="@dimen/padding">

    <LinearLayout
        android:id="@+id/gpick_ll_text_holder"
        android:orientation="vertical"
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <TextView
            android:id="@+id/gpick_group_name"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            style="@style/CardViewBoldNormal"
            tools:text="Group name"/>

        <TextView
            android:id="@+id/gpick_group_description"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textSize="@dimen/txt_small"
            tools:text="Description" />

        <TextView
            android:id="@+id/gpick_item_last_event"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textSize="@dimen/txt_small"
            tools:text="Last event" />

    </LinearLayout>

    <ImageView
        android:id="@+id/iv_Selected"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_alignParentRight="true"
        android:layout_alignTop="@id/gpick_ll_text_holder"
        android:layout_alignBottom="@id/gpick_ll_text_holder"
        android:src="@drawable/btn_unchecked"
        android:layout_marginRight="20dp" />

</RelativeLayout>